---
k8s_packages:
  - package: kubeadm
    version: "{{ kubernetes_version | default('1.19.7-0') }}"
  - package: kubectl 
    version: "{{ kubernetes_version | default('1.19.7-0') }}"
  - package: kubelet
    version: "{{ kubernetes_version | default('1.19.7-0') }}"
